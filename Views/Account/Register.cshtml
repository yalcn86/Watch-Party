@model WhatchParty.Models.User

@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>IKARUS // PROTOCOL_REGISTER</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@500;600;700&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --bg-main: #0a0a0f;
            --bg-card: #12121a;
            --cyber-cyan: #00f0ff;
            --cyber-yellow: #ffec00;
            --cyber-red: #ff0055;
            --text-main: #f1f5f9;
            --text-muted: #64748b;
            --border-color: #222235;
        }

        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            background-color: var(--bg-main);
            color: var(--text-main);
            font-family: 'Rajdhani', sans-serif;
            background-image: radial-gradient(circle at 50% 50%, #141425 0%, #0a0a0f 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .register-frame {
            background: var(--bg-card);
            border: 1px solid var(--border-color);
            border-top: 4px solid var(--cyber-yellow);
            padding: 40px;
            width: 100%;
            max-width: 450px;
            box-shadow: 0 15px 35px rgba(255, 236, 0, 0.03);
            position: relative;
        }

        .register-frame::before {
            content: 'v0.1.5';
            position: absolute;
            top: 15px;
            right: 20px;
            color: var(--text-muted);
            font-family: monospace;
            font-size: 0.8rem;
        }

        .title {
            font-family: 'Orbitron', sans-serif;
            font-size: 1.8rem;
            letter-spacing: 2px;
            margin-bottom: 5px;
            color: #fff;
            text-shadow: 0 0 10px var(--cyber-yellow);
        }

        .subtitle {
            color: var(--text-muted);
            font-size: 0.9rem;
            margin-bottom: 30px;
            text-transform: uppercase;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            font-family: 'Orbitron', sans-serif;
            font-size: 0.8rem;
            color: var(--cyber-yellow);
            margin-bottom: 8px;
            letter-spacing: 1px;
        }

        .form-group input {
            width: 100%;
            background: var(--bg-main);
            border: 1px solid var(--border-color);
            padding: 12px 15px;
            color: #fff;
            font-family: monospace;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-group input:focus {
            outline: none;
            border-color: var(--cyber-yellow);
            box-shadow: 0 0 10px rgba(255, 236, 0, 0.15);
        }

        .cyber-btn {
            width: 100%;
            padding: 15px;
            font-family: 'Orbitron', sans-serif;
            font-size: 1rem;
            font-weight: 700;
            background: var(--cyber-yellow);
            color: #000;
            border: none;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 1px;
            transition: all 0.3s ease;
            margin-top: 10px;
        }

        .cyber-btn:hover {
            background: #fff;
            box-shadow: 0 0 15px var(--cyber-yellow);
        }

        .alert-error {
            background: rgba(255, 0, 85, 0.1);
            border: 1px solid var(--cyber-red);
            color: var(--cyber-red);
            padding: 12px;
            font-size: 0.9rem;
            margin-bottom: 20px;
            font-family: monospace;
        }

        .redirect-link {
            text-align: center;
            margin-top: 25px;
            font-size: 0.9rem;
            color: var(--text-muted);
        }

        .redirect-link a {
            color: var(--cyber-cyan);
            text-decoration: none;
            font-weight: bold;
        }

        .redirect-link a:hover {
            text-shadow: 0 0 8px var(--cyber-cyan);
        }
    </style>
</head>
<body>

    <div class="register-frame">
        <h1 class="title">NEW_IDENTITY</h1>
        <p class="subtitle">Siber ağ kimliğinizi oluşturun</p>

        @if (ViewBag.Error != null)
        {
            <div class="alert-error">> REG_FAILED: @ViewBag.Error</div>
        }

        <form action="/Account/Register" method="POST">
            <div class="form-group">
                <label>IDENTITY_NAME</label>
                <input type="text" name="Username" required placeholder="Kullanıcı adı seçin...">
            </div>

            <div class="form-group">
                <label>NETWORK_EMAIL</label>
                <input type="email" name="Email" required placeholder="E-posta adresinizi girin...">
            </div>

            <div class="form-group">
                <label>KEY_PASSWORD</label>
                <input type="password" name="Password" required placeholder="En az 6 karakter...">
            </div>

            <button type="submit" class="cyber-btn">INITIALIZE PROTOCOL</button>
        </form>

        <div class="redirect-link">
            Zaten kayıtlı mısınız? <a href="/Account/Login">SIGN_IN</a>
        </div>
    </div>

</body>
</html>